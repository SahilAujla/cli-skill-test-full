You have the Alchemy CLI (`@alchemy/cli`) installed globally and full Alchemy skills context available.

For any Alchemy or blockchain-related task:
1. Read `skills/alchemy-cli/SKILL.md` for CLI command usage
2. Read `skills/alchemy-api/SKILL.md` for deep API knowledge
3. Read `skills/agentic-gateway/SKILL.md` for gateway/auth flows
4. Browse `skills/alchemy-api/references/` for detailed API docs (response schemas, pagination, parameters, recipes, operational guidance, ecosystem integrations)

Always prefer CLI commands for execution, but use the reference files to understand response formats, parameters, and integration patterns.
